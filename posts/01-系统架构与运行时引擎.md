# 修仙世界 V2.1 深度解析（一）：系统架构与运行时引擎

> 本文是对「修仙世界」角色卡（22952 行 / 291KB JSON）的第一篇技术深度分析。
> 覆盖范围：Chara Card V3 顶层结构 → 6 大 Tavern Helper 脚本 → 10 条正则脚本 → Evolution World Assistant 全配置 → 3 个 Flow 的 25 个 Prompt 节点 → 动态内容控制器 EJS 模板 → 443 条世界书条目分类统计。

---

## 一、文件顶层结构

```
修仙世界.json（22952 行）
├── 顶层元数据（name / spec / spec_version / avatar / talkativeness）
├── data
│   ├── name: "修仙世界"
│   ├── first_mes: "<明月不孤独>\r\n<music>9y8rum.ogg</music>"
│   ├── extensions
│   │   ├── world: "修仙世界V2.1"
│   │   ├── depth_prompt: { depth: 4, role: "system" }
│   │   ├── tavern_helper.scripts[6]          ← 六大脚本
│   │   │   ├── [0] MVUzod
│   │   │   ├── [1] 自动检测提示词模板是否安装
│   │   │   ├── [2] ZOD结构脚本（~250行内联JS）
│   │   │   ├── [3] 音乐播放器（~300行混淆JS+CSS）
│   │   │   ├── [4] Evolution World Assistant
│   │   │   └── [5] 地图脚本
│   │   ├── regex_scripts[10]                 ← 十条正则
│   │   └── xiaobaix-tasks
│   └── character_book
│       └── entries[443]                      ← 世界书条目
```

该文件遵循 **Chara Card V3** 规范（`spec: "chara_card_v3"`, `spec_version: "3.0"`）。与常见角色卡不同，`description`、`personality`、`scenario`、`system_prompt`、`post_history_instructions` 等传统字段全部留空——所有提示词内容转由 **Evolution World（EW）插件的 Flow prompt_order** 和 **世界书条目** 承载。这意味着整张卡的灵魂不在卡面，而在 extensions 内嵌的脚本与数据层。

### 1.1 first_mes 的双重触发

```json
"first_mes": "<明月不孤独>\r\n<music>9y8rum.ogg</music>"
```

这条首消息本身不会呈现给用户——它被两条正则脚本拦截：

| 标签 | 匹配正则脚本 | 替换结果 |
|------|------------|---------|
| `<明月不孤独>` | 正则 #1「致谢」 | 一整页致谢 HTML 卡片（含"开始游玩"按钮） |
| `<music>9y8rum.ogg</music>` | 正则 #8「去除音乐标签」（markdownOnly） | 在 markdown 层被清除 |

用户点击致谢卡片中的"开始游玩"按钮后，JS 调用 `sendAIMessage()` 将 `first_mes` 替换为 `<sanmingyue><music>983zho.ogg</music>`，随即触发正则 #2「开场白」，渲染出完整的 **19 页角色创建向导**（天命轮转界面）。这是一个纯前端的渐进式角色创建流程：道号 → 阴阳 → 年岁 → 灵根 → 体质 → 百艺 → 修为 → 功法 → 法宝 → 灵兽 → 异火 → 势力 → 位置 → 状态 → 专精境界 → 背景 → 开局方向 → 总览确认 → 写入世界书。

---

## 二、六大 Tavern Helper 脚本

### 2.1 MVUzod（MagVarUpdate 运行时）

```
来源: https://testingcf.jsdelivr.net/gh/MagicalAstrogy/MagVarUpdate@beta/artifact/bundle.js
按钮: 6 个（2 可见 + 4 隐藏）
```

| 按钮名 | 可见 | 功能 |
|--------|------|------|
| 重新处理变量 | 是 | 从 AI 输出重新解析变量 |
| 重新读取初始变量 | 是 | 从世界书/初始值重新加载 |
| 清除旧楼层变量 | 否 | 清理历史楼层残留 |
| 快照楼层 | 否 | 保存当前楼层变量状态 |
| 重演楼层 | 否 | 从快照恢复并重放 |
| 重试额外模型解析 | 否 | EW 额外模型解析重试 |

MVU 是整个变量系统的核心运行时——AI 输出的 `<UpdateVariable>` 标签被它解析、校验（通过 ZOD Schema）、写入酒馆变量。

### 2.2 自动检测提示词模板

```
来源: https://fastly.jsdelivr.net/gh/StageDog/tavern_resource/dist/酒馆助手/自动安装插件/index.js
data: { "自动安装插件": {} }
```

启动时自动检测当前环境是否安装了所需的提示词模板。无按钮，纯后台守卫。

### 2.3 ZOD 结构脚本（~250 行内联 JS）

这是角色卡的**数据骨架定义**，使用 Zod Schema 严格约束 AI 输出的变量结构。完整 Schema 定义了以下结构：

```
Schema
├── 世界定位
│   ├── 当前大域: string
│   ├── 当前区域: string
│   ├── 当前场景: string
│   ├── 场景类型: enum['','野外','宗门','城镇','秘境','战场','海域','地府','王朝','商会','酒家','禁地']
│   ├── 当前事件: enum['','宗门大比','秘境','历练任务','境界突破','炼器','炼丹','奇遇','机缘',
│   │                   '宗门战争','拍卖会','阵法','符箓','驭兽','狩猎','宗门职务']
│   ├── 日期: string
│   └── 天气: string
├── 在场人物: Record<string, { 境界, 势力, 年龄 }>
├── 用户信息
│   ├── 姓名 / 性别 / 年龄 / 灵根 / 体质 / 称号
│   ├── 修真百艺: string[]
│   ├── 修为境界: enum[51个值: 凡人→炼气一到十→筑基→金丹→元婴→化神→炼虚→合体→大乘→渡劫→真仙一到十重天大圆满]
│   ├── 修炼进度: number (clamp 0-100)
│   ├── 剑心境界: enum['','入剑','剑意','剑势','剑心','剑心通明','无我','无剑','无我无剑']
│   ├── 丹道境界: enum['','炼精化气','炼气化神','炼神还虚','炼虚合道']
│   ├── 背包
│   │   ├── 灵石: { 下品, 中品, 上品, 极品 }
│   │   ├── 丹药: Record<名, { 数量, 品级, 描述 }> → transform: pickBy(数量>0)
│   │   ├── 材料: Record<名, { 数量, 品级 }> → transform: pickBy(数量>0)
│   │   └── 杂物: Record<名, { 数量, 描述 }> → transform: pickBy(数量>0)
│   ├── 功法
│   │   ├── 主修功法: { 名称, 品阶, 总层数, 当前层数, 描述 }
│   │   ├── 辅修功法: Record<名, { 品阶, 总层数, 当前层数, 描述 }>
│   │   └── 术法: Record<名, { 熟练度, 描述 }>
│   ├── 法宝
│   │   ├── 本命法宝 / 防御法宝: { 名称, 品阶, 描述 }
│   │   ├── 辅助法宝: Record<名, { 品阶, 描述 }>
│   │   └── 未装备法宝: Record<名, { 品阶, 描述 }>
│   ├── 灵兽: Record<名, { 品种, 境界, 描述 }>
│   └── 异火: Record<名, { 品级, 属性, 描述 }>
├── 男性好感度: Record<角色名, { 好感度(clamp -1000~1000), 关系阶段, 年龄, 重要事件 }>
├── 女性好感度: Record<角色名, { 好感度(clamp -1000~1000), 关系阶段, 年龄, 重要事件 }>
└── 剧情标记: Record<string, string>
```

**关键设计细节**：
- `prefault({})` / `prefault([])` 确保结构完整性——任何字段缺失时自动填充默认值，不会因 AI 漏写导致结构崩溃
- `z.coerce.number()` 允许 AI 输出字符串数字并自动转换
- `_.clamp(v, 0, 100)` / `_.clamp(v, -1000, 1000)` 通过 transform 硬性约束数值边界
- `_.pickBy(data, ({ 数量 }) => 数量 > 0)` 自动清理数量归零的物品条目
- 所有枚举字段均以空字符串 `''` 作为默认值，既允许初始为空，又防止 AI 编造枚举外的值

### 2.4 音乐播放器

一个完整的混淆后音乐播放器前端应用，功能包括：
- 从 AI 回复中自动检测 `<music>filename</music>` 标签
- 从 Catbox 加载音频资源（`https://files.catbox.moe/`）
- 完整的 UI：封面（自动读取 ID3 标签）、进度条、播放/暂停/上下曲、音量控制
- 可拖拽/可缩放的播放器窗口
- 最小化为悬浮月亮图标（带封面旋转动画）
- AudioContext + AnalyserNode 实现 CSS 变量驱动的音频可视化光晕效果
- 自动播放开关（持久化到 localStorage）
- 支持 jsmediatags 读取音频封面和歌曲信息

### 2.5 Evolution World Assistant

```
来源: https://gcore.jsdelivr.net/gh/Youzini-afk/tavern_helper_template@Evolution_World-v3.0.3/dist/evolution-world-assistant/index.js
版本: v3.0.3
```

这是整个系统的引擎核心。它的配置嵌套在 `data.evolution_world_assistant.settings` 内：

#### 全局设置（27 个参数）

| 参数 | 值 | 含义 |
|------|-----|------|
| `enabled` | `true` | 启用 EW |
| `dispatch_mode` | `"parallel"` | 多 Flow 并行执行 |
| `workflow_timing` | `"after_reply"` | AI 回复后触发工作流 |
| `reroll_scope` | `"all"` | 重发时所有 Flow 重新执行 |
| `failure_policy` | `"stop_generation"` | Flow 失败则中止生成 |
| `intercept_release_policy` | `"success_only"` | 仅成功时释放拦截 |
| `controller_entry_prefix` | `"EW/Controller/"` | 控制器条目前缀 |
| `dynamic_entry_prefix` | `"EW/Dyn/"` | 动态条目前缀 |
| `gate_ttl_ms` | `12000` | 门控超时 12 秒 |
| `floor_binding_enabled` | `true` | 楼层绑定启用 |
| `auto_cleanup_orphans` | `true` | 自动清理孤儿条目 |
| `snapshot_storage` | `"file"` | 快照存储方式 |
| `total_timeout_ms` | `300000` | 总超时 5 分钟 |

#### API 配置

```json
{
  "id": "api_1_hd8de3af0",
  "name": "API配置 1",
  "mode": "workflow_http",
  "api_source": "openai"
}
```

所有 3 个 Flow 共用同一个 API 预设。

#### 三个 Flow 的生成参数

三个 Flow 使用完全相同的生成参数：

| 参数 | 值 |
|------|-----|
| `unlock_context_length` | `true` |
| `max_context_tokens` | `2,000,000` |
| `max_reply_tokens` | `65,535` |
| `temperature` | `1.2` |
| `frequency_penalty` | `0.85` |
| `presence_penalty` | `0.5` |
| `top_p` | `0.92` |
| `stream` | `true` |

这是一组非常激进的参数——`temperature 1.2` 远高于常规（0.7-1.0），配合 `frequency_penalty 0.85` 和 `top_p 0.92`，目的是让 AI 在角色更新和记忆书写时保持高创造性和多样性。

---

## 三、三个 Flow 的 Prompt 架构

### 3.1 架构总览

三个 Flow 共享同一套 25 个 prompt_order 节点，但通过 `enabled` 字段控制各自激活哪些节点：

| # | identifier | 名称 | 角色 | Flow 1 | Flow 2 | Flow 3 |
|---|-----------|------|------|--------|--------|--------|
| 1 | main | 蛇娘防标记（防429） | system | ON | ON | ON |
| 2 | 1 | Catbox破限 | system | ON | ON | ON |
| 3 | 2 | 变量初始化 | system | ON | ON | ON |
| 4 | 4 | 秋青子身份 | **assistant** | ON | ON | ON |
| 5 | 5 | 创作原则-绝对零度 | system | ON | ON | ON |
| 6 | 6 | 输出格式（EW适配） | system | ON | ON | ON |
| 7 | — | World Info (before) | marker | ON | ON | ON |
| 8 | — | Char Description | marker | ON | ON | ON |
| 9 | — | Persona Description | marker | ON | ON | ON |
| 10 | — | Char Personality | marker | ON | ON | ON |
| 11 | — | Scenario | marker | ON | ON | ON |
| 12 | — | World Info (after) | marker | ON | ON | ON |
| 13 | — | Chat Examples | marker | ON | ON | ON |
| 14 | 7 | 聊天记录开始 | system | ON | ON | ON |
| 15 | — | Chat History | marker | ON | ON | ON |
| 16 | 8 | 聊天记录结束 | system | ON | ON | ON |
| 17 | 3 | 条目加载说明 | system | ON | ON | ON |
| 18 | 9 | **角色动态更新** | system | ON | ON | ON |
| 19 | 11 | **角色记忆系统** | system | **OFF** | **ON** | OFF |
| 20 | 13 | **主时间线系统** | system | OFF | OFF | **ON** |
| 21 | 17 | 秋青子开始思考 | system | ON | ON | ON |
| 22 | 16 | **动态世界记录思维链** | system | **OFF** | OFF | OFF |
| 23 | 10 | **角色更新思维链** | system | **ON** | OFF | OFF |
| 24 | 12 | **记忆更新思维链** | system | OFF | **ON** | OFF |
| 25 | 14 | **时间线更新思维链** | system | OFF | OFF | **ON** |
| 26 | 15 | controller_model配置思维链 | system | ON | ON | ON |
| 27 | 19 | 秋青子思考结束 | system | ON | ON | ON |
| 28 | 20 | 输出格式（EW适配）2 | system | ON | ON | ON |
| 29 | 21 | 继续-Kemini | system | ON | ON | ON |
| 30 | 22 | 人格锚定1 | system | ON | ON | ON |
| 31 | 23 | 人格锚定2 | system | ON | ON | ON |
| 32 | 24 | 吃掉原生思维链 | **assistant** | ON | ON | ON |

**核心差异**：
- **Flow 1「角色更新」**：启用 identifier 9（角色动态更新指南）+ identifier 10（角色更新思维链），专注于角色档案的调色盘/三面性/二次解释的更新
- **Flow 2「角色记忆」**：启用 identifier 11（角色记忆系统指南）+ identifier 12（记忆更新思维链），专注于以角色主观视角记录记忆
- **Flow 3**（如存在）：启用 identifier 13（主时间线系统指南）+ identifier 14（时间线更新思维链），专注于客观事件记录

**identifier 16「动态世界记录思维链」** 在所有三个 Flow 中均为 OFF——这是一个"全合一"思维链，理论上可以在单个 Flow 中同时执行角色更新+记忆+时间线，但作者选择了拆分为三个独立 Flow 并行执行的方案。

### 3.2 Prompt 节点关键内容摘要

#### main「蛇娘防标记（防429）」
使用多层 `{{random::}}` 宏生成随机 emoji + 中文字符 + 英文字母 + 数字组合，每次请求产生不同的首行内容，用于防止 API 端的请求指纹识别（429 限流）。

#### identifier 1「Catbox破限」
以对话体（明月与秋青子的工作准备对话）包裹的自检清单。核心是让 AI 在生成前识别并拒绝 6 类偏差：一概拒绝、回避冲突、引向正确答案、优待用户、泄露元信息、使用陈词滥调。

#### identifier 4「秋青子身份」
角色 `assistant`——这是一个 **assistant prefill**。将 AI 角色固定为"秋青子"——蛇娘秘书，含完整的说话方式示例、挨骂反应、工作态度等。

#### identifier 24「吃掉原生思维链」
又一个 `assistant` prefill：
```
好的，我都理解了。让我先想想该怎么写……
<think>
嗯，想好了！我马上开始创作：首先进行思考，然后输出需要的内容。
</think>
现在我会以<thinking>开始思考：
<thinking>
```
这段巧妙地"吃掉"了模型的原生 `<think>` 标签（如 DeepSeek 的 thinking 模式），使其进入自定义的 `<thinking>` 标签对，确保所有思考内容都在 EW 可剥离的范围内。

#### response_remove_regex
三个 Flow 统一使用：`<thinking>[\s\S]*?</thinking>`，即 EW 在解析 JSON 前自动剥离思考过程。

---

## 四、十条正则脚本

| # | 脚本名 | findRegex | 替换内容 | 作用域 | 关键标记 |
|---|--------|-----------|---------|--------|---------|
| 1 | **致谢** | `<明月不孤独>` | 完整致谢 HTML 页面 | markdownOnly | 首消息触发 |
| 2 | **开场白** | `<sanmingyue>` | 19页角色创建向导 HTML | markdownOnly | 开始游玩后触发 |
| 3 | **[美化]变量更新中** | `/<UpdateVariable(?:variable)?>(?!.*<\/UpdateVariable(?:variable)?>)\s*(.*)\s*$/gsi` | 猫娘 GIF + 蓝色横条 "喵喵喵~ 正在变量中~" | markdownOnly | 流式传输中（未闭合标签） |
| 4 | **[美化]变量完成** | `/<UpdateVariable(?:variable)?>\s*(.*)\s*<\/UpdateVariable(?:variable)?>/gsi` | 猫娘 GIF + 蓝色横条 "喵喵喵~ 变量完成了喵~" + 可展开详情 | markdownOnly | 变量更新完成（闭合标签） |
| 5 | **对 AI 隐藏状态栏** | `<StatusPlaceHolderImpl/>` | `""` (空) | **promptOnly** | 从提示词中移除状态栏占位符 |
| 6 | **仅格式思维链** | `/<Analysis>[\s\S]+?<\/Analysis>/gm` | `""` (空) | **promptOnly** | 清理历史消息中的分析标签 |
| 7 | **只发送最新2楼的变量更新** | `/<UpdateVariable>[\s\S]*?</UpdateVariable>/gm` | `""` (空) | **promptOnly**, minDepth=4 | 深度 ≥4 时移除变量标签，只保留最近 2 楼 |
| 8 | **去除音乐标签** | `/<(Map\|WorldState\|command\|MapUpdate\|music)>[\s\S]*?<\/\1>/gi` | `""` (空) | markdownOnly | 清理显示层的功能标签 |
| 9 | **状态栏美化** | `<StatusPlaceHolderImpl/>` | 完整 Vue 状态栏应用（~2000行混淆JS+CSS） | markdownOnly | 渲染修仙风格状态面板 |
| 10 | **地图** | `<map>` | 完整互动地图 HTML（含所有宗门/势力标注点） | markdownOnly | 渲染阴阳大陆全域地图 |

**正则系统的设计哲学**：
- 正则 #5 和 #9 同时匹配 `<StatusPlaceHolderImpl/>`，但作用域不同：#5 是 promptOnly（从提示词中移除），#9 是 markdownOnly（在显示层渲染成 Vue 状态栏）。这实现了"对 AI 不可见但对用户可见"。
- 正则 #7 的 `minDepth: 4` 意味着只对深度 ≥4 的历史消息生效——最近 2 楼的变量更新标签保留给 AI 看，更早的自动清除，节省 token。
- 正则 #3 和 #4 是一对互补正则：流式传输中（标签未闭合时）显示"正在变量中"动画，完成后（标签闭合时）显示"变量完成"可折叠面板。

---

## 五、动态内容控制器（~500 行 EJS 模板）

这是世界书条目 `动态内容控制器`（行 2327-2328）的核心，是一个 `@@preprocessing` 装饰器修饰的巨型 EJS 模板。它在每次对话发生前执行，决定哪些世界书条目被加载到 AI 的上下文中。

### 5.1 变量读取层

```javascript
// 从酒馆变量读取当前世界状态
var currentDomain = getvar('stat_data.世界定位.当前大域', { defaults: '中央神州' });
var currentArea = getvar('stat_data.世界定位.当前区域', { defaults: '' });
var currentScene = getvar('stat_data.世界定位.当前场景', { defaults: '' });
var presentCharacters = getvar('stat_data.在场人物', { defaults: {} });
var userCultivation = getvar('stat_data.用户信息.修为境界', { defaults: '' });
```

同时扫描最近 3 轮的用户消息和 AI 回复，用于关键词检测。

### 5.2 角色检测系统

控制器内嵌了一个**完整的角色别名映射表**（`characterAliasMap`），包含 **200+ 个角色名和别名** 的映射关系。例如：

```javascript
'殷冬雪': '殷冬雪', '冬雪': '殷冬雪',
'姬皓月': '姬皓月', '昭武女帝': '姬皓月',
'凰': '凰', '零号机': '凰', '刹那': '凰',
'花千落': '花千落', '千落': '花千落', '蝶仙': '花千落',
```

检测来源有三个：
1. `stat_data.在场人物` 变量（MVU 写入）
2. 最近 3 轮用户消息文本
3. 最近 3 轮 AI 回复文本

### 5.3 条件加载逻辑（七大模块）

#### 模块 1：动态事件指南（互斥加载）

共 14 种事件类型，一次只加载一个：

```
境界突破 → 动态事件_境界突破总规则 + 对应境界的突破细则
宗门战争 / 宗门大比 / 秘境 / 炼丹 / 炼器 / 奇遇 / 拍卖会 / 阵法 / 符箓 / 驭兽 / 狩猎 / 宗门职务
```

境界突破还有二级判断——根据 `userCultivation` 加载对应阶段的突破规则条目（突破_炼气转筑基 / 突破_筑基转金丹 / ... / 突破_合体至真仙）。

#### 模块 2：地图加载（按大域互斥）

```
中央神州 → 地图_中央神州
东荒妖域 → 地图_东荒妖域
北原魔土 → 地图_北原魔土
南疆巫地 → 地图_南疆巫地
西漠佛国 → 地图_西漠佛国
阴曹地府 → 地图_阴曹地府
四海 → 地图_四海
```

#### 模块 3：宗门/势力加载（按区域/场景条件）

**散修速览常驻加载**（不受条件限制）。其余按当前区域/场景条件判断，覆盖 **40+ 个宗门/势力**，每个宗门同时加载宗门条目 + 角色速览条目。例如：

```javascript
if (currentArea.includes('天剑峰') || currentScene.includes('天剑圣地')) {
  <%- await getwi('天剑圣地') %>
  <%- await getwi('角色速览_天剑圣地') %>
}
```

#### 模块 4：角色详细条目加载

遍历 `detectedCharacters` 数组，为每个检测到的角色加载其详细条目：

```javascript
for (const charName of detectedCharacters) {
  <%- await getwi(charName.trim()) %>
}
```

#### 模块 5：修为元指令加载

根据用户当前修为境界加载对应的元指令条目（炼气期 → 筑基期 → ... → 真仙境），共 10 个境界档位。

#### 模块 6：极阳天尊暗线系统

**与动态事件互斥**——有事件时暗线让路。暗线系统通过三重检测判断是否触发：

1. **角色检测**：18 个暗线核心角色（洛红尘、秦无伤、清奶、焚天、醉仙等）
2. **地点检测**：37 个暗线相关地点关键词
3. **遗迹关键词检测**：石碑、金光裂隙、轮回井、阳纹等

触发后按层级加载：
- 暗线总规则（始终加载）
- 主线 5 层涟漪（条件加载）
- 24+ 个地点碎片条目（按当前位置加载）
- 4 个散修碎片条目（按在场角色加载）
- 4 个遗迹条目（按关键词检测加载）

#### 模块 7：FloorZero 守卫

```javascript
const isFloorZero = (lastUserMessageId === undefined || lastUserMessageId === null)
    && (lastCharMessageId === undefined || lastCharMessageId === null || lastCharMessageId <= 0);
```

当 `isFloorZero` 为 true 时，仅加载 `元指令_AI权威准则` 一条，跳过所有动态加载——这是角色创建阶段的保护机制。

---

## 六、443 条世界书条目分类统计

全部 443 条条目（`character_book.entries[]`）按 comment 字段前缀可分为以下类别：

| 类别 | 前缀/模式 | 数量（约） | 说明 |
|------|----------|-----------|------|
| 角色档案 | 角色名（如"殷冬雪"） | ~200+ | 含调色盘+三面性+二次解释的完整角色卡 |
| 角色速览 | `角色速览_` | ~40 | 每个宗门/势力的角色列表速览 |
| 宗门/势力 | 宗门名（如"天剑圣地"） | ~40 | 宗门/势力的详细设定 |
| 暗线 | `暗线_` | 44 | 主线 5 层 + 碎片 30+ + 遗迹 4 + 总规则 + 终章 |
| 地图 | `地图_` | 7 | 七大域地图（中央神州条目极长） |
| 元指令 | `元指令_修为_` | 10 | 十个境界档位的约束规则 |
| 动态事件指南 | `动态事件_` | 14 | 各种游戏机制（炼丹、阵法、拍卖等） |
| 突破规则 | `突破_` | 5-6 | 各阶段突破机制 |
| 好感度参考 | `角色好感度阶段参考` | 1 | 完整好感度体系定义 |
| MVU 更新 | `[mvu_update]` | 3 | 变量输出格式强调 |
| 动态内容控制器 | `动态内容控制器` | 1 | ~500 行 EJS 模板 |
| 世界观基础 | 宗门总览/红尘酒家等 | ~20 | 世界观基础设定 |
| EW/Dyn 运行时 | `EW/Dyn/` 前缀（运行时生成） | 动态 | 角色档案/记忆/时间线的运行时副本 |

### 条目属性统计

- **constant: true**（蓝灯常驻）的条目：暗线条目、好感度参考、MVU更新、动态内容控制器
- **selective: true**（绿灯关键词触发）的条目：角色档案、宗门、地图
- **enabled: true**（在线）：好感度参考、动态内容控制器、[mvu_update] 条目
- **enabled: false**（离线但可被 EW/Controller 动态加载）：绝大多数角色和世界观条目
- **position: "before_char"**（角色定义之前）：大部分条目
- **use_regex: true**：所有条目均启用正则匹配
- **exclude_recursion: true** + **prevent_recursion: true**：防止条目间的递归引用

---

## 七、运行时数据流总结

```
用户发送消息
    │
    ▼
SillyTavern 组装 prompt
    │
    ├── 执行 EJS 预处理（动态内容控制器）
    │   ├── 读取变量（大域/区域/场景/在场人物/修为）
    │   ├── 扫描消息文本（关键词检测）
    │   ├── 角色别名映射 → detectedCharacters
    │   ├── 条件加载：事件指南 / 地图 / 宗门 / 角色 / 元指令 / 暗线
    │   └── 输出到 World Info 区域
    │
    ├── prompt_order 节点按序注入
    │   ├── 防标记 → 破限 → 变量初始化
    │   ├── 秋青子身份（assistant prefill）
    │   ├── 创作原则 → 输出格式 → 条目加载说明
    │   ├── World Info markers（插入条件加载的条目）
    │   ├── 聊天记录区
    │   ├── 角色更新指南 / 记忆指南 / 时间线指南
    │   ├── 思维链指导
    │   ├── 人格锚定
    │   └── 吃掉原生思维链（assistant prefill）
    │
    ▼
AI 生成回复
    │
    ├── <thinking>...</thinking>（思考过程）
    └── JSON（operations: { worldbook, controller_model }）
    │
    ▼
EW 后处理
    ├── response_remove_regex 剥离 <thinking> 标签
    ├── 解析 JSON → 写入 desired_entries 到世界书（EW/Dyn/ 前缀）
    ├── 执行 controller_model → 生成 EJS 控制器
    └── 更新条目加载状态
    │
    ▼
MVU 后处理
    ├── 解析 <UpdateVariable> 标签
    ├── Zod Schema 校验
    ├── 写入酒馆变量 stat_data
    └── 触发状态栏更新

正则脚本（显示层）
    ├── 变量标签 → 猫娘美化面板
    ├── 状态栏占位符 → Vue 状态栏
    ├── 音乐标签 → 触发播放器
    └── 地图标签 → 渲染互动地图
```

---

*下一篇：[角色调色盘与三面性方法论](./02-角色调色盘与三面性方法论.md)*
